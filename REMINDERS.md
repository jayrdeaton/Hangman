# Reminders

Ideas parked for later — not yet implemented.

- **Pass & play "Ready" (handoff) screen needs more ceremony.** Feels a little flat right now.
  (`PnpHandoffDialog`.) The icon's color is done — now uses `secondary`/`onSecondary`.
