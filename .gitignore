# dependencies
node_modules/

# Expo
.expo/
web-build/
ios/
android/
expo-env.d.ts
dist
build*

# AI Readme files
readmes/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files (real secrets — .env.example is the checked-in template)
.env
.env*.local

# scraper build artifacts — raw scrape output / build-time-only intermediate
# data, never read by the shipped app; regenerate locally via the scripts/
# scrapers if needed. See src/data/puzzleCatalog.generated.ts for what the
# app actually bundles.
/JEOPARDY.json
/data/puzzlesAll.json

# typescript
*.tsbuildinfo

# testing
coverage/

# vscode
.vscode/exponentIndex.js

# vercel
.vercel
