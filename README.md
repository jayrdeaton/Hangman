# Hangman

Puzzle content (`data/*.json`) is generated by a private companion repo and copied in here — see
`scripts/buildPuzzleCatalog.ts` (`npm run build:catalog`) for how it gets bundled into the app at
build time via `src/data/puzzleCatalog.generated.ts`.
